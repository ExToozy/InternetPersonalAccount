<Window
    x:Class="ViewLayer.Views.RegistrationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ViewLayer.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="RegistrationView"
    Width="1000"
    Height="600"
    ResizeMode="NoResize"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid Background="#e2edf6" MouseDown="Grid_MouseDown">

        <Button
            Name="CloseBtn"
            Margin="5"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Background="#0c5eaa"
            Click="CloseBtn_Click">
            <Button.Content>
                <materialDesign:PackIcon Margin="3" Kind="Close" />
            </Button.Content>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <Button
            Name="MinimazeBtn"
            Margin="5,5,32,5"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Background="#0c5eaa"
            Click="MinimazeBtn_Click">
            <Button.Content>
                <materialDesign:PackIcon Margin="3" Kind="Minus" />
            </Button.Content>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>

        <Border
            Margin="300,30,300,30"
            Background="White"
            CornerRadius="30">
            <Border.Effect>
                <DropShadowEffect
                    BlurRadius="30"
                    ShadowDepth="0"
                    Color="LightGray" />
            </Border.Effect>

            <Grid Margin="40,20,40,20">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    VerticalAlignment="Center"
                    Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                    Text="Регистрация"
                    TextAlignment="Center" />
                <TextBox
                    Name="UserPhoneNumberTextBox"
                    Grid.Row="1"
                    VerticalAlignment="top"
                    materialDesign:HintAssist.Hint="Номер телефона"
                    materialDesign:HintAssist.HelperText="Начиная с 8"
                    materialDesign:HintAssist.HelperTextFontSize="12"
                    FontSize="20"
                    Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                <TextBox
                    Name="UserFullNameTextBox"
                    Grid.Row="2"
                    VerticalAlignment="Top"
                    materialDesign:HintAssist.Hint="ФИО"
                    FontSize="20"
                    Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                <TextBox
                    Name="UserPasswordTextBox"
                    Grid.Row="3"
                    VerticalAlignment="Top"
                    materialDesign:HintAssist.Hint="Пароль"
                    FontSize="20"
                    Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                <TextBox
                    Name="UserPasswordRetryTextBox"
                    Grid.Row="4"
                    VerticalAlignment="Top"
                    materialDesign:HintAssist.Hint="Повторите пароль"
                    FontSize="20"
                    Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                <StackPanel Grid.Row="5">
                    <Button
                        Name="RegisterBtn"
                        Height="40"
                        Margin="0,10,0,5"
                        Background="#0c5eaa"
                        Click="RegisterBtn_Click"
                        FontSize="20">

                        <Button.Content>
                            <TextBlock Foreground="White" Text="Зарегестрироваться" />
                        </Button.Content>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button
                        VerticalAlignment="Top"
                        VerticalContentAlignment="Top"
                        FontSize="15"
                        Style="{StaticResource MaterialDesignFlatButton}">
                        <Button.Content>
                            <TextBlock Foreground="#0c5eaa" Text="Войти" />
                        </Button.Content>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
